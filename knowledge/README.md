# Knowledge corpus

`demo_records.jsonl` is a synthetic fixture for local development and the M0
retrieval suite. It is not a company handbook.

Do not replace it with real policies, credentials, personal data, or restricted
source material in a public clone. Keep private imports on disk and out of Git;
generated ledgers belong under `artifacts/`, which is ignored.
